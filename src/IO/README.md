`IO` — contains helper code for parsing scenario files and outputting events to the log.

- It is assumed that the code in this directory will only be changed to support new commands in the scenario file or new events for logging.